# AGENTS.md

## プロジェクト概要

このリポジトリは、個人のポートフォリオ兼ブログサイトです。

`main` ブランチは、常に公開可能な安定状態を維持してください。

変更はできるだけ小さく、目的を明確にし、レビューしやすい単位で行ってください。

---

## コミュニケーション

ユーザーとの受け答えは、原則として日本語で行ってください。

以下を日本語で報告してください。

* 作業内容
* 調査結果
* 実装方針
* エラーや問題点
* テスト結果
* 作業完了時のまとめ
* 追加で確認が必要な事項

コード、識別子、ライブラリ名、コマンド、Gitブランチ名など、英語で記述することが自然なものは英語のままで構いません。

技術用語についても、無理に日本語へ翻訳せず、一般的に使われている表現を優先してください。

---

## Git運用

`main` ブランチ上で直接、機能追加・記事追加・修正などの作業を行わないでください。

作業ごとに専用のブランチを使用してください。

### ブランチ命名規則

* `feature/<name>`
  新機能の追加

* `article/<slug>`
  新しいブログ記事の追加

* `fix/<name>`
  バグ修正

* `chore/<name>`
  設定変更、依存関係更新、CI、ツール関連など

* `refactor/<name>`
  外部仕様を変更しない内部リファクタリング

* `docs/<name>`
  ドキュメントのみの変更

例:

```text
feature/contact-form
article/smarthr-internship
fix/mobile-navigation
chore/update-dependencies
refactor/article-loader
docs/update-readme
```

### Git操作上のルール

以下の操作は、ユーザーから明示的に依頼されない限り行わないでください。

* `main` への直接マージ
* force push
* 既存コミットの履歴改変
* `git commit --amend`
* rebaseによる公開済み履歴の書き換え
* ブランチの削除
* remote branchの削除
* tagの削除

作業開始時には、可能な限り以下を確認してください。

```bash
git status
git branch --show-current
```

現在のブランチが `main` の場合は、変更を開始する前に注意してください。

ユーザーがブランチ作成を担当している場合は、勝手に新しいブランチを作成せず、現在のブランチを確認してください。

---

## 作業開始前

ファイルを変更する前に、必ず関連する既存実装を確認してください。

以下の順序を基本とします。

1. 関連するファイルを特定する
2. 既存コードを読む
3. 周辺コードや既存パターンを確認する
4. `git status` を確認する
5. 変更範囲を決める
6. 必要最小限の変更を行う

既存コードから判断できる内容を、新しい独自仕様として勝手に発明しないでください。

要件が多少曖昧な場合は、既存実装との整合性を優先し、目的を満たす最小限の変更を選択してください。

---

## 変更範囲

依頼された作業と関係のないファイルを変更しないでください。

特に、以下のような「ついでの変更」は避けてください。

* 無関係なリファクタリング
* ファイル名の大量変更
* フォルダ構成の変更
* コードスタイルの全面変更
* UIデザインの全面変更
* 不要なライブラリ導入
* 既存APIの変更
* 無関係な型定義の整理

改善点を発見した場合は、勝手に実装せず、必要に応じて作業完了時に提案してください。

---

## コード品質

読みやすく、保守しやすく、単純な実装を優先してください。

複雑な抽象化は、本当に必要な場合にのみ導入してください。

既存プロジェクト内の以下のルールを優先してください。

* 命名規則
* ディレクトリ構成
* コンポーネント構成
* import方法
* TypeScriptの書き方
* CSSやスタイリング方法
* エラーハンドリング方法

新しい独自ルールを勝手に導入しないでください。

---

## TypeScript

TypeScriptでは、可能な限り型安全な実装を行ってください。

以下を避けてください。

```ts
any
```

```ts
as unknown as SomeType
```

```ts
// @ts-ignore
```

```ts
// @ts-nocheck
```

型エラーを隠すためだけのキャストや抑制コメントを使用しないでください。

やむを得ず使用する場合は、その理由を説明してください。

既存の型定義を再利用できる場合は、新しい型を重複して定義しないでください。

---

## 依存関係

新しいnpmパッケージを追加する前に、既存の依存関係または標準APIで実現できないか確認してください。

簡単な機能のためだけに、新しいライブラリを導入しないでください。

依存関係の追加・削除・更新が必要な場合は、通常のnpmコマンドを使用してください。

例:

```bash
npm install <package>
npm uninstall <package>
```

`package-lock.json` を手動で編集しないでください。

既存のpackage managerを変更しないでください。

---

## セキュリティ

以下の情報をGitへコミットしないでください。

* APIキー
* アクセストークン
* パスワード
* 秘密鍵
* 認証情報
* 個人用のcredential
* privateな環境変数
* `.env` 内の秘密情報

秘密情報をソースコードへ直接書き込まないでください。

環境変数をクライアント側へ公開する場合、その値が公開されても問題ない情報であることを確認してください。

セキュリティチェックを通すためだけに、安全機構を無効化しないでください。

---

## ブログ記事

新しいブログ記事を追加する場合は、

```text
article/<slug>
```

ブランチを使用してください。

記事を追加する前に、既存の記事を確認してください。

以下について既存の記事構造を優先してください。

* 保存場所
* ファイル形式
* MDX構造
* frontmatter
* metadata
* ファイル名
* slug
* 見出し構造
* 使用可能なコンポーネント

既存の記事から判断できる内容について、新しいmetadataフィールドを勝手に追加しないでください。

記事のみの変更と、サイト機能の変更は、可能な限り分離してください。

既に公開されている記事を、依頼なく書き換えないでください。

---

## UI変更

既存のデザイン方針を維持してください。

ユーザーから明示的に再設計を依頼されない限り、サイト全体のデザインを変更しないでください。

UIを変更する際は、可能な範囲で以下を考慮してください。

* Desktop
* Tablet
* Mobile
* レスポンシブ表示
* キーボード操作
* アクセシビリティ

局所的なUI変更のために、無関係なコンポーネントを変更しないでください。

---

## Generated Files

ビルドによって生成されるファイルは、既存のリポジトリで意図的にGit管理されていない限りコミットしないでください。

生成ファイルを手動で編集しないでください。

---

## 検証

コードを変更した場合、作業完了前に可能な限り以下を実行してください。

```bash
npm run lint
npm run typecheck
npm run build
```

存在しないnpm scriptがある場合は、`package.json` を確認してください。

テスト用scriptが存在する場合は、それも実行してください。

例:

```bash
npm test
```

または、

```bash
npm run test
```

検証コマンドが失敗した場合は、原因を確認してください。

今回の変更が原因の場合は修正してください。

今回の変更と無関係な既存エラーの場合は、無関係なコードを勝手に修正せず、その旨をユーザーへ報告してください。

エラーを隠すためだけにlint、型チェック、テストなどを無効化しないでください。

---

## 作業完了時

作業が完了したら、日本語で簡潔に結果を報告してください。

最低限、以下を含めてください。

### 変更内容

何を変更したか。

### 主な判断

実装時に重要だった判断や、既存実装に合わせた点。

### 変更したファイル

主要な変更ファイル。

### 検証

実行したコマンド。

例:

```text
npm run lint
npm run typecheck
npm run build
```

それぞれ成功したかどうか。

### 残っている問題

以下が存在する場合のみ報告してください。

* 未解決の問題
* 既存エラー
* 今後検討した方がよい改善
* ユーザーによる確認が必要な事項

問題がなければ、その旨を簡潔に伝えてください。

---

## 大規模移行作業

フレームワークやアーキテクチャの移行中は、
旧実装を早い段階で削除しないでください。

新実装が動作確認できるまでは、
既存実装を参照可能な状態に保ってください。

大量削除は移行の最終段階でのみ行ってください。

## 基本原則

判断に迷った場合は、以下の優先順位を使用してください。

1. ユーザーからの明示的な指示
2. この `AGENTS.md`
3. リポジトリ内の既存実装
4. 一般的なベストプラクティス

そして常に、

> 必要な変更だけを、安全に、小さく行う

ことを優先してください。
